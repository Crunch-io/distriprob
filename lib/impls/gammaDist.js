"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * (C) Copyright John Maddock 2006.
 * (C) Copyright Zachary Martin 2016 (port to javascript).
 * Use, modification and distribution are subject to the
 * Boost Software License:
 *
 * Permission is hereby granted, free of charge, to any person or organization
 * obtaining a copy of the software and accompanying documentation covered by
 * this license (the "Software") to use, reproduce, display, distribute,
 * execute, and transmit the Software, and to prepare derivative works of the
 * Software, and to permit third-parties to whom the Software is furnished to
 * do so, all subject to the following:
 *
 * The copyright notices in the Software and this entire statement, including
 * the above license grant, this restriction and the following disclaimer,
 * must be included in all copies of the Software, in whole or in part, and
 * all derivative works of the Software, unless such copies or derivative
 * works are solely in the form of machine-executable object code generated by
 * a source language processor.
 *
 * THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
 * IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
 * FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
 * SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
 * FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
 * ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
 * DEALINGS IN THE SOFTWARE.
 *
 */
var Gamma = require("./boostPorts/special_functions/gamma").Gamma;
var IGammaInverse = require("./boostPorts/special_functions/detail/igamma_inverse").IGammaInverse;
var Random = require("./random").Random;
var check = require("./errorHandling").check;
var GammaDist = /** @class */ (function () {
    function GammaDist() {
    }
    GammaDist.checkParameters = function (functionName, shape, scale, x, lowerTail, p, n, seed) {
        var params = {
            lowerTail: undefined
        };
        check(shape, "shape", "gamma distribution " + functionName, "positive_real");
        check(scale, "scale", "gamma distribution " + functionName, "positive_real");
        if (typeof x !== "undefined") {
            check(x, "x", "gamma distribution " + functionName, "real");
        }
        if (typeof lowerTail !== "undefined" && lowerTail !== null) {
            check(lowerTail, "lowerTail", "gamma distribution " + functionName, "boolean");
            params.lowerTail = lowerTail;
        }
        else {
            params.lowerTail = true;
        }
        if (typeof p !== "undefined") {
            check(p, "p", "gamma distribution " + functionName, "probability");
        }
        if (typeof n !== "undefined") {
            check(n, "n", "gamma distribution " + functionName, "nonnegative_integer");
        }
        if (typeof seed !== "undefined" && seed !== null) {
            check(seed, "seed", "gamma distribution " + functionName, "seed");
        }
        return params;
    };
    GammaDist.pdf = function (x, shape, scale) {
        // first error checking
        var params = GammaDist.checkParameters("pdf", shape, scale, x);
        if (x <= 0) {
            return 0;
        }
        else if (x === 0) {
            if (shape < 1) {
                return Number.POSITIVE_INFINITY;
            }
            else if (shape === 1) {
                return 1 / scale;
            }
            else {
                return 0;
            }
        }
        else {
            return Gamma.gamma_p_derivative(shape, x / scale) / scale;
        }
    };
    GammaDist.cdf = function (x, shape, scale, lowerTail) {
        // first error checking
        var params = GammaDist.checkParameters("cdf", shape, scale, x, lowerTail);
        if (x <= 0) {
            if (params.lowerTail) {
                return 0;
            }
            else {
                return 1;
            }
        }
        else {
            var a = x / scale;
            if (params.lowerTail) {
                return Gamma.gamma_p(shape, a);
            }
            else {
                return Gamma.gamma_q(shape, a);
            }
        }
    };
    GammaDist.quantile = function (p, shape, scale, lowerTail) {
        // first error checking
        var params = GammaDist.checkParameters("quantile", shape, scale, undefined, lowerTail, p);
        if (p === 0) {
            if (params.lowerTail) {
                return 0;
            }
            else {
                return Number.POSITIVE_INFINITY;
            }
        }
        else if (p === 1) {
            if (params.lowerTail) {
                return Number.POSITIVE_INFINITY;
            }
            else {
                return 0;
            }
        }
        else {
            if (params.lowerTail) {
                return IGammaInverse.gamma_p_inv(shape, p) * scale;
            }
            else {
                return IGammaInverse.gamma_q_inv(shape, p) * scale;
            }
        }
    };
    GammaDist.random = function (n, shape, scale, seed) {
        // first error checking
        var params = GammaDist.checkParameters("quantile", shape, scale, undefined, undefined, undefined, n, seed);
        return Random.numbers(n, GammaDist.quantile, [shape, scale, true], seed);
    };
    GammaDist.randomIterator = function (n, shape, scale, seed) {
        if (typeof n === "undefined" || n === null) {
            n = Number.POSITIVE_INFINITY;
        }
        // first error checking
        var params = GammaDist.checkParameters("quantile", shape, scale, undefined, undefined, undefined, n, seed);
        return Random.numberIterator(n, GammaDist.quantile, [shape, scale, true], seed);
    };
    return GammaDist;
}());
exports.GammaDist = GammaDist;
