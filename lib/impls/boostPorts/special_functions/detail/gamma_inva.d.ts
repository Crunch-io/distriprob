export declare class GammaInvA {
    private static gamma_inva_t(z, p, invert);
    static inverse_poisson_cornish_fisher(lambda: number, p: number, q: number): number;
    private static gamma_inva_imp(z, p, q);
    static gamma_p_inva(x: number, p: number): number;
    static gamma_q_inva(x: number, q: number): number;
}
