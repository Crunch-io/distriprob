export declare class IGammaInverse {
    private static find_inverse_s(p, q);
    private static didonato_SN(a, x, N, tolerance?);
    private static didonato_FN(p, a, x, N, tolerance);
    private static find_inverse_gamma(a, p, q);
    private static gamma_p_inverse_func(a, p, inv);
    private static gamma_p_inv_imp(a, p);
    private static gamma_q_inv_imp(a, q);
    static gamma_p_inv(a: number, p: number): number;
    static gamma_q_inv(a: number, q: number): number;
}
