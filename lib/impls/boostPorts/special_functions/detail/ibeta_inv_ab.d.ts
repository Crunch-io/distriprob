export declare class IBetaInvAB {
    private static beta_inv_ab_t(b, z, p, invert, swap_ab);
    static inverse_negative_binomial_cornish_fisher(n: number, sf: number, sfc: number, p: number, q: number): number;
    private static ibeta_inv_ab_imp(b, z, p, q, swap_ab);
    static ibeta_inva(b: number, x: number, p: number): number;
    static ibetac_inva(b: number, x: number, q: number): number;
    static ibeta_invb(a: number, x: number, p: number): number;
    static ibetac_invb(a: number, x: number, q: number): number;
}
